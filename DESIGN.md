# switchproxy 设计文档

> 本文面向想理解 switchproxy **为什么这样设计**的读者。它不是 API 手册（那是 README 的事），
> 而是完整传达设计意图：每一个结构为什么存在、为什么是这个形态而不是别的形态。
> 文中引用格式为 `文件:行号`，可对照源码阅读。

---

## 1. 问题域：switchproxy 解决什么

场景：你在使用一个**闭源/不可配置**的代理客户端（下称"上级代理"），它只给你一个本地端口，
不告诉你哪些流量走了它、走了多少、能不能挑选某些流量绕开它。

switchproxy 插入在应用与上级代理之间，做且只做一件事：

```
应用（浏览器/系统代理）
        │
        ▼
  switchproxy ─── 规则引擎 ──┬── DIRECT ──▶ 本机真实出口
        │                   └── PROXY ───▶ 上级代理（HTTP CONNECT / SOCKS5）
        │                   └── REJECT ──▶ 就地拒绝
        ▼
   流量观测（实时面板 + SQLite 历史）
```

它是一个**分流中间件 + 观测探针**，不是又一个代理实现。这个定位决定了后面所有设计：

- 它不需要节点订阅、协议实现、TUN 接管——那些是上级代理的事；
- 它必须**零侵入**地知道每个连接去了哪、用了多少流量——观测是一等公民，不是附加功能。

## 2. 设计哲学（总纲）

五条原则，按优先级排列，冲突时靠前者胜出：

1. **单二进制，零运行时依赖。** 编译产物即全部：代理、统计、存储、API、前端页面全部在一个可执行文件里。拷到任何 Linux/Windows 上直接跑，不需要装 Node、不需要 nginx、不需要先初始化数据库。这是 Go 生态的经典自包含思路（Prometheus、Syncthing 同款），但对本地小工具来说收益更大——用户甚至不需要知道"前端"和"后端"是两个东西。
2. **纯隧道，不解密。** 流量原样转发，无 MITM、无证书安装、无内容解析。观测面只采集**元数据**（域名、端口、规则、字节数），永远不碰内容。这既是隐私承诺，也是复杂度控制：解密意味着证书管理、TLS 栈、协议对抗，一个都不能要。
3. **热路径极简。** 每个字节的转发路径上只允许：一次 `io.Copy`、一层 conn 包装里的原子加法。所有贵的操作（JSON 序列化、SQLite 写入、排序）都被推到旁边的周期性协程里，绝不阻塞转发。
4. **有界内存。** 一切集合都有上限：速率窗口 120 秒、最近关闭连接 500 条、快照下发 100 条、历史保留 30/90 天。进程跑一年，内存占用曲线应该是平的。
5. **体量匹配的抽象。** 全项目约 1500 行 Go + 600 行前端，`package main` 平铺四个文件，无框架、无接口层、无依赖注入。中间件链、插件系统在这个体量是过度设计——能用装饰器和过程式流水线解决的，不引入更重的形式。

## 3. 总体架构：一个进程，四个平面

```
┌──────────────────────────── switchproxy 进程 ────────────────────────────┐
│                                                                          │
│  转发平面（数据通路）                观测平面（控制通路）                    │
│  ┌─────────────────────┐           ┌──────────────────────────────┐      │
│  │ 入站 :7899 混合端口   │           │ stats 全局统计器（内存）        │      │
│  │  ├ HTTP 代理         │  register │  ├ 连接表 / 聚合表 / 速率窗口  │      │
│  │  └ SOCKS5           │──────────▶│  └ sampleLoop 每秒速率采样     │      │
│  │ 规则引擎 matchAction │  addUp/   │                              │      │
│  │ 出站 dialer          │  addDown  │ history SQLite（分钟级落盘）    │      │
│  │  ├ DIRECT 直连       │◀──────────│  ├ traffic_points / 域名按天   │      │
│  │  └ PROXY 上级        │ unregister│  └ retention 每日清理          │      │
│  │ tunnel + countingConn│           │                              │      │
│  └─────────────────────┘           │ admin :9090（面板 + REST + WS）│      │
│                                    │  └ go:embed web/dist 前端      │      │
│                                    └──────────────────────────────┘      │
└──────────────────────────────────────────────────────────────────────────┘
                                             │
                                             ▼ （唯一的"外部"：浏览器）
```

四个平面的职责与文件对应：

| 平面 | 文件 | 职责 | 运行模型 |
|---|---|---|---|
| 转发 | main.go | 入站协议、规则、出站、隧道 | 每连接一个 goroutine |
| 采集 | stats.go | 计数、聚合、速率 | 转发协程内联 + 1 个采样协程 |
| 存储 | history.go | SQLite 增量落盘、保留策略 | 2 个周期协程 |
| 展示 | admin.go + web/ | REST API、WS 推送、页面 | 1 个 HTTP 服务 + hub 协程 |

**关键结构性决策：采集与转发同进程同热路径。** 没有独立的 collector/sidecar——统计就是代理在转发时顺手做的埋点。收益：零 IPC、零额外部署、计数天然精确（就是同一份 `net.Conn` 上的字节）；代价：代理停则采集停、只统计流经本实例的流量。对本地工具来说这两个代价都不成立为问题。

## 4. 转发平面设计

### 4.1 混合入站：一字节探测（main.go:445-457)

同一端口同时接受 HTTP 代理和 SOCKS5，方法是在 `handleConn` 里 `Peek(1)` 一个字节：

- `0x05` → SOCKS5（版本字节恰好是 5）
- 其他 → HTTP（任何 HTTP 方法名的首字母都 ≠ 0x05）

这不是奇技淫巧，而是**两个协议首字节恰好不相交**的必然利用。收益：用户不需要记两个端口、不需要配错协议；实现只要一个 `bufio.Reader` 先偷看再分发，缓冲不浪费。

### 4.2 HTTP 入站：CONNECT 与明文分治（main.go:263-355）

- **CONNECT（HTTPS，主流情况）**：解析出目标 host:port → 规则匹配 → 拨号 → 回 `200 Connection Established` → 进入纯隧道。之后一个字节都不看。
- **明文 HTTP**：作为显式代理解析请求行和头，重建请求转发。这里有一个协议义务：直连或 SOCKS5 上级时请求行必须改写为 origin-form（`GET /path HTTP/1.1`），HTTP 上级则保持 absolute-form 原样（main.go:341-344）。

两者最终都汇入同一个 `tunnel()`，差别只在拨号方式——**连接一旦建立，协议差异即消失**，这是"纯隧道"理念的体现。

### 4.3 SOCKS5 入站：只实现最小集（main.go:363-436)

握手只支持无认证（`0x00`）和 CONNECT（`0x01`）命令。BIND/UDP ASSOCIATE 直接回不支持。本地回环场景不需要更多；每少一个特性就少一片攻击面和代码路径。

### 4.4 规则引擎：有序列表，命中即停（main.go:125-147）

采用 Clash/Surge 的规则行格式（`TYPE,VALUE,ACTION`），从上到下线性扫描：

- **为什么用行业格式**：用户已有的分流规则可以直接粘贴，零学习成本。
- **为什么线性扫描不用 trie/AC 自动机**：规则数量是个位到百位级，线性扫描的缓存友好性足以碾压任何花哨结构。数据结构升级的门槛应该由真实规模触发，而不是想象中的规模。
- **语义边界（有意为之）**：`IP-CIDR` 只对** IP 字面量目标**生效，不做主动 DNS 解析。域名类规则只对域名目标生效（`ip == nil` 守卫）。解耦 DNS 意味着不引入解析时延、不产生额外查询，代价是无法匹配"解析后落在某网段的域名"——这个取舍在 README 的"说明与限制"里向用户明示。

### 4.5 出站拨号：三种动作统一为一个函数（main.go:214-225）

`dialByAction` 把 DIRECT / PROXY(http) / PROXY(socks5) 收敛为同一个签名 `func(addr) (net.Conn, error)`。 tunnel 不关心流量最终走了哪条路——**差异在连接建立时刻终结，之后只有字节流**。

HTTP 上级拨号（main.go:156-195）注意两个细节：

- CONNECT 阶段设 15 秒 deadline，隧道建立后 `SetDeadline(time.Time{})` 清零——**超时只用于握手，不污染数据面**；
- 用 `bufio.Reader` 读响应头时可能多缓冲了字节，`tunnel()` 开头的 `Peek(Buffered())` 回放（main.go:240-244）保证不丢数据。

### 4.6 隧道：最朴素的形态（main.go:229-255)

两个 goroutine 各自 `io.Copy`，任一方向结束即整体收尾。没有超时、没有空闲检测、没有半关闭接力——TCP 自身的 FIN/RST 就是流控信号，应用层不重复发明。这个函数是全项目的热路径核心，它的每一行都被刻意压到最简。

## 5. 观测平面设计

### 5.1 采集的核心机制：conn 装饰器（stats.go:328-348)

**这是全项目最重要的一处模式选择。** 字节计数的实现是 `countingConn`：

```go
type countingConn struct {
    net.Conn                        // 装饰器：包装任意 net.Conn
    onRead  func(int64)             // 从该连接读到 n 字节
    onWrite func(int64)             // 向该连接写入 n 字节
}
```

`tunnel()` 把 client/server 两侧各包一层，读回调算上行、写回调算下行（main.go:237-238）：

- **为什么是装饰器而不是中间件链**：中间件（`handler(next)` 组合）解决的是**请求级**横切关注点；这里关注点是**字节级**的，正确的作用点在 `net.Conn` 接口上。装饰器模式在此与 `io.Reader/Writer` 生态天然咬合：包一层 conn，`io.Copy` 无感知地开始计数。
- **可叠加性**：将来要加限速、加密、连接级日志，再包一层 conn 即可，互不干扰。
- **语义精确性**：包装在两个端点上而非一个，使"上行/下行"的语义来自方向（client 侧读=上行，server 侧读=下行），而不是猜测。

### 5.2 热路径的并发策略（stats.go:172-196)

`addUp/addDown` 在每次 Read/Write 回调里执行，策略是刻意的三层分裂：

| 计数目标 | 同步手段 | 理由 |
|---|---|---|
| 连接级 up/down、全局总量 | `atomic.Int64.Add` | 无锁，最便宜，最热 |
| 域名/规则聚合 | 单把 `aggMu` 互斥 | 写多读少，map 必须锁 |
| 连接表 conns | `RWMutex` | 写一次（register/unregister），读频繁（快照） |

已知取舍：聚合锁是全局单点，万级并发连接时会成为串行化瓶颈。但在本工具的目标规模（本地、百级连接）下，锁竞争的成本远低于更复杂结构（分片 map、无锁聚合）的代码成本。**并发结构跟着预期规模走，不跟着极限规模走。**

### 5.3 数据模型：连接的一生（stats.go:130-169)

```
dial 成功 ──▶ register()：入连接表，域名/规则计数 +1
     │            │
     │        每次读写 ──▶ addUp/addDown：三处原子累加
     │            │
连接关闭 ◀── unregister()：移出连接表，快照进 closed 环（上限 500）
```

`ConnMeta` 持有 `client net.Conn`（stats.go:27）不是为转发，是为管理端**强制关闭连接**（stats.go:290-301）——观测面反控数据面，这是"探针"升格为"工具"的最低配置。

### 5.4 速率采样：差分法（stats.go:304-324)

不做每连接计时器，而是每秒读一次全局总量原子数、与上一秒做差。所有速率（全局与单连接，后者在前端算，见 §8）都是**对累计值的差分**——一个时钟、一份状态，没有分布式的计时器丛林。窗口 120 秒，够画图，不够占内存。

### 5.5 快照：读侧的全量物化（stats.go:280-288)

`Snapshot()` 把观测状态一次性物化为纯 JSON 结构（overview + 活跃连接 + 最近关闭 + Top15 域名 + Top10 规则）。它是观测面对外的**唯一**出口形态：REST 和 WS 推的都是它。一份模型，两种消费方式，不会漂移。

## 6. 存储平面：SQLite 历史库（history.go)

### 6.1 驱动选择：纯 Go，无 CGO（history.go:10)

用 `modernc.org/sqlite` 而不是 `mattn/go-sqlite3`。决定性理由：**交叉编译**。build.sh 要出 Windows 产物（`CGO_ENABLED=0 GOOS=windows`），CGO 驱动会直接锁死这条路。纯 Go 驱动慢 2-3 倍，但本场景每分钟才写一次，性能余量是无限的。**用编译自由换运行性能，在"每分钟 6 行写入"的负载下是免费的选择。**

### 6.2 落盘模型：增量差分（history.go:88-135)

每分钟对齐到整点（+20ms 防边界竞态）：

- 全局总量与上次落盘值做差 → UPSERT 进 `traffic_points`（按分钟为主键累加）；
- 域名聚合表做同样的快照差分 → UPSERT 进 `domain_stats`（按天）。

**存增量而不是存全量**，是内存模型（累计值）与磁盘模型（时间序列）之间的翻译层：重启后 `lastTotals` 归零，天然从新起点继续累加，不需要任何恢复逻辑。UPSERT 累加语义则让"同一分钟内多次落盘"幂等无害。

### 6.3 保留策略与查询降采样（history.go:138-211)

- 流量点留 30 天、域名统计留 90 天，每日一次清理——**磁盘占用有界**，与内存有界同一条原则。
- 查询侧按范围降采样：1h→1 分钟桶、24h→10 分钟桶、7d/30d→1 小时桶，用 SQL 的 `GROUP BY (minute/?)` 完成。分钟级存储 + 任意粒度聚合，避免了为每种视图预建汇总表。

## 7. 管理端设计（admin.go)

### 7.1 独立端口，默认仅本机（main.go:106-108)

数据面（7899）与观测面（9090）分离：面板挂了不影响代理，代理的重负载不拖累面板。默认绑 `127.0.0.1` 是安全底线——管理 API 无认证，绝不能暴露到网络上。

### 7.2 WS Hub：单次序列化，扇出推送（admin.go:99-117)

推送模型（server push）而非轮询（poll），理由：

- 面板需要的是**每秒一致的一帧**，轮询会造成多客户端时间轴错位；
- `loop()` 每秒 `json.Marshal` **一次**，同一份字节发给所有客户端——序列化成本与客户端数量解耦。

慢客户端策略是此设计的点睛之笔：每个客户端的 `send` channel 只有 4 帧缓冲，塞满即踢（admin.go:110-112）。**观测面绝不缓冲数据面的过去**——落后的消费者被丢弃而不是被堆积，内存依然有界。`readLoop` 只为探测断连（`SetReadLimit(1024)`），协议本身单向。

### 7.3 前端内嵌与 SPA 回退（admin.go:19, 144-160)

`go:embed all:web/dist` 把构建产物打进二进制；dist 里预置一个"前端尚未构建"占位页，保证不构建前端也能编译（`go:embed` 的硬性要求），并把补救动作直接写在占位页上。静态服务对未知路径回退 `index.html`，支撑前端路由刷新不 404。

## 8. 前端设计（web/）

技术形态：Vue 3 + Vite + ECharts，四个视图（Overview / Connections / Tops / History）各自消费快照的一部分。两个值得说明的设计：

- **useWs.ts：单例响应式状态源。** 模块级 `reactive` 的 `wsState` 被所有视图共享，WS 连接与重连（指数退避，上限 3 秒）是模块副作用，只发生一次。组件只读状态，不拥有连接——**连接的生命周期挂在页面生命周期之上**，切视图不断流。
- **useConnRates.ts：速率计算下沉到前端。** 服务端快照只有累计值，单连接实时速率由前端对相邻两帧做差分（500ms 检查周期）。这是热路径原则的延伸：**能不算在服务端就不算在服务端**，且天然复用了 §5.4 的差分思想——前后端共享同一个心智模型。

开发期工作流：Vite dev server（5173）把 `/api`、`/ws` 反代到 9090（web/vite.config.ts:8-11），前端热更新与后端解耦；生产期同一份代码 embed 进二进制。**开发态与运行态的代码是同一份，只有传输方式不同。**

## 9. 配置与生命周期（main.go:466-480, 482-535)

- **热重载**：2 秒轮询 mtime，变更则重新解析并 `atomic.Value.Store` 整个新 `Config` 指针。新连接立刻用新规则，**存量隧道不受影响**（它们手里的是旧 conn，规则只在拨号时刻消费一次）。轮询而非 inotify：跨平台（Windows build）零成本。
- **pid 文件**：写在配置文件旁边（`config.yaml.pid`，main.go:119），随配置走、随进程生灭——这是给运维脚本的最小契约，不引入 lock/daemon 库。
- **退出**：SIGINT/SIGTERM 即退出，不做连接排空。本地代理的连接短、重连廉价，优雅退出的复杂度不划算。

## 10. 关键决策索引（ADR 摘要）

| # | 决策 | 备选方案 | 决定性理由 |
|---|---|---|---|
| 1 | 采集内嵌在代理进程 | 独立 collector / sidecar | 零 IPC、零部署项；代价（代理停则采集停）在本地场景不成立 |
| 2 | conn 装饰器做字节计数 | 中间件链 / 旁路镜像 | 关注点在字节级，作用点必须在 `net.Conn`；与 io 生态天然咬合 |
| 3 | WS 每秒全量快照推送 | REST 轮询 / 增量事件流 | 多客户端时间轴一致；单次序列化扇出；实现最简 |
| 4 | 纯 Go SQLite 驱动 | CGO 驱动 | 交叉编译出 Windows 产物是硬需求；写入负载极低，性能损失无感 |
| 5 | 混合端口（Peek 探测） | 双端口分开 | 协议首字节不相交，探测免费；用户体验一个地址 |
| 6 | 永不解密流量 | MITM + 根证书 | 隐私承诺即复杂度控制；元数据已满足观测需求 |
| 7 | 一切集合有界 | 无界增长/外部缓存 | 长期运行的内存可预测性；本地场景无需持久化细粒度全量 |
| 8 | 规则线性扫描 | trie / hash 索引 | 规模（≤百条）下山不过缓存友好性；拒绝想象性优化 |
| 9 | 慢客户端直接踢除 | 无界发送缓冲 | 观测面不欠数据面的过去；内存有界优先于投递完整 |
| 10 | 每分钟差分落盘 | 实时逐事件写库 | 磁盘写入与连接数解耦；重启零恢复逻辑 |

## 11. 安全与隐私边界

- **不碰内容**：不解密、不缓存、不记录 payload；日志与统计里只有元数据（host/port/规则/字节数）。
- **默认回环监听**：两个端口都绑 127.0.0.1，管理 API 无认证的前提就是不出本机。
- **已知松弛（有意接受）**：WS `CheckOrigin` 全放行（admin.go:40）——本机单用户场景，严格 Origin 检查只会制造兼容性摩擦而无安全收益；若未来面板需要暴露到局域网，此项与 API 认证必须一起补上。

## 12. 已知限制（设计边界，非缺陷）

- 透明度为**系统代理级**：只接管遵守系统代理的应用，无视代理的进程不受影响（全局接管需 TUN，超出定位）。
- `IP-CIDR` 仅匹配 IP 字面量目标，无主动 DNS 解析（见 §4.4）。
- SOCKS5 入站仅无认证 + CONNECT。
- 管理端无认证、无 TLS，仅限本机使用。
- 明文 HTTP 代理转发时按协议要求改写请求行为 origin-form（不可逆但合规）。

## 13. 代码地图

```
main.go     转发平面：配置解析、规则引擎、HTTP/SOCKS5 入站、出站拨号、隧道、热重载
stats.go    采集平面：ConnMeta、聚合表、速率采样、countingConn 装饰器、快照物化
history.go  存储平面：SQLite 打开/建表、分钟差分落盘、保留清理、范围查询降采样
admin.go    展示平面：WS Hub、REST API、静态文件服务、SPA 回退、go:embed
web/src/    前端：四个视图 + useWs（单例状态源）+ useConnRates（前端差分速率）
build.sh    构建：web → linux 二进制 → windows 二进制（CGO_ENABLED=0）
```

阅读顺序建议：`handleConn`（main.go:445）→ `serveHTTP`（main.go:263）→ `tunnel`（main.go:229）→
`countingConn`（stats.go:328）→ `Stats`（stats.go:102）→ `recordLoop`（history.go:88）→
`wsHub.loop`（admin.go:99）。这条线就是一条连接从生到死、从字节到像素的完整旅程。
